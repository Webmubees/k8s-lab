apiVersion: v1
kind: Service
metadata:
  name: general-service
  labels:
    app: general-service
spec:
  selector:
    app: general-service
  ports:
    - name: http
      port: 80
      targetPort: 8000